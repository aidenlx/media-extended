---
title: Quick Start
description: Getting started with Media Extended v4
icon: Album
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { File, Folder, Files } from 'fumadocs-ui/components/files';
import { 
  ObsidianInstallInfoCard, 
  BRATInstallInfoCard, 
  ManualInstallInfoCard, 
  ReleaseZip,
  getDefaultMethodIndex
} from '@/components/ui/manifest';

## Introduction

Welcome to the docs for Media Extended v4 beta!

Integrate, manage, and play media files directly in Obsidian. 🖇️🗂️⏯️

### For User from v3 [#v3-user]

<Callout type="warn" title="License Change">
Future releases of v4 will be **closed source**.
The original codebase remains open source under the MIT license and can be found in the [`v3` branch](https://github.com/aidenlx/media-extended/tree/v3).
</Callout>

If you're coming from earlier versions or planning to join the beta testing, there are some important changes you should be aware of. 
We recommend starting with the [breaking changes](/docs/v4/breaking-changes) to understand what's different in v4, 
then check out the [new features](/docs/v4/whats-new) to see what's new.

## Installation [#install]

<Tabs
  items={["Obsidian", "BRAT", "Manual"]}
  defaultIndex={await getDefaultMethodIndex(
    { id: "media-extended", repo: "aidenlx/media-extended", version: "v4" }, 
    [
      { label: "Obsidian", value: "obsidian" },
      { label: "BRAT", value: "brat" },
      { label: "Manual", value: "manual" },
    ])
  }
>
  <Tab>

### via Obsidian [!toc]

<ObsidianInstallInfoCard version="v4" />

<div className='fd-steps [&_h4]:fd-step'>

#### Enable community plugin [!toc]

1. Open `Settings` > `Community plugin`
2. Turn on community plugin

#### Install [!toc]

1. Open [plugin page](https://obsidian.md/plugins?id=media-extended) in obsidian
2. Click `Install`
3. Wait for installation to complete

#### Enable [!toc]

Click `Enable` Button

</div>
  </Tab>
  <Tab>

### via BRAT [!toc]

<BRATInstallInfoCard />

<Callout type="warn"> Beta versions are not production-ready, make sure to make proper backup before preceeding. </Callout>

You can use the Beta Reviewers Auto-update Tester (BRAT) plugin to install a pre-release version of the plugin.

<div className='fd-steps [&_h4]:fd-step'>

#### Install BRAT [!toc]

Download [BRAT](https://obsidian.md/plugins?id=obsidian42-brat) and enable it

#### Add a beta plugin for testing [!toc]

1. Open [link to BRAT](obsidian://brat?plugin=aidenlx/media-extended), it will open BRAT and ask you to add the plugin. 
   Of course, you can manually add the plugin by using the `Add Beta Plugin` command and enter `aidenlx/media-extended`.
2. Click on `Add Plugin` -- wait a few seconds and BRAT will tell you what is going on

#### Enable the beta plugin [!toc]

1. After BRAT confirms the installation, Open `Settings` > `Community plugin`
2. Refresh the list of plugins
3. Find the beta plugin you just installed and Enable it.

</div>
  </Tab>
  <Tab>

### Manual Install [!toc]

<ManualInstallInfoCard channel="beta" />

<Callout type="warn">
This method is not recommended. Only try manual install if you have trouble
with all other methods.
</Callout>

<div className='fd-steps [&_h4]:fd-step'>
#### Download [!toc]

Download `media-extended.zip` from GitHub Release: 

<ReleaseZip channel="beta">
  Click here to download
</ReleaseZip>

#### Enable community plugin [!toc]

1. Open `Settings` > `Community plugin`
2. Turn on community plugin

#### Open plugin folder [!toc]

- If you haven't installed any plugin before, you need to open the plugin folder manually: `<vault>/.obsidian/plugins`
- If you have installed other plugins, you can use `Open plugin folder` button on the right of the `Installed plugins` section

<Callout>
The `.obsidian` folder may be hidden. On macOS, you should be able to press `Command+Shift+Dot` to show the folder in Finder.
</Callout>

#### Create plugin folder [!toc]

Create a new folder `media-extended` and put files from `media-extended.zip` into the folder

Finally, the folder structure should look like this:

<Files>
  <Folder name="<vault>/.obsidian/plugins" open>
    <Folder name="media-extended" open>
      <File name="main.js" />
      <File name="manifest.json" />
      <File name="styles.css" />
    </Folder>
  </Folder>
</Files>

Make sure three files are in the plugin folder: `main.js`, `manifest.json` and `styles.css`

### Reload & Enable [!toc]

1. Refresh the list of plugins or restart Obsidian if you have installed other plugins
2. Open `Settings` > `Community plugin` and find `Media Extended` in the list
3. Enable the plugin using toggle

</div>
  </Tab>
</Tabs>

## Getting Started [#getting-started]

With just one click, you can open video directly in Obsidian.

Click this link to try it now: [Steve Jobs' 2005 Stanford Commencement Address](obsidian://mx-open?url=https%3A%2F%2Fwww.youtube.com%2Fwatch%3Fv%3DUF8uR6Z6KLc)

![first player with youtube video](./first-player.png)

See that **Timestamp&nbsp;⭐&nbsp;** button? When you're ready to take notes, one click adds a timestamped link back to this exact moment in the video. It's that easy!

![first media note](./first-note.png)

You're all set! To continue, you can check out the following guides:

import { 
  FolderOpenIcon,
  LibraryIcon,
  FileTextIcon,
  ListMusicIcon,
  ListOrderedIcon,
  PlayCircleIcon,
  LinkIcon,
  HelpCircleIcon 
} from 'lucide-react';

<Cards>
  <Card icon={<FolderOpenIcon />} href="/docs/v4/load-media" title="Opening Media">
    Open media from local files, remote URLs, and media-hosting services
  </Card>
  <Card icon={<LibraryIcon />} href="#" title="Media Library">
    Manage your media in Media Library
  </Card>
  <Card icon={<FileTextIcon />} href="#" title="Transcript">
    Quick navigation using transcript
  </Card>
  <Card icon={<ListMusicIcon />} href="#" title="Playlist">
    Create and manage playlists
  </Card>
  <Card icon={<ListOrderedIcon />} href="#" title="Chapter">
    Generate chapters for your media
  </Card>
  <Card icon={<PlayCircleIcon />} href="#" title="Playback">
    Customize playback settings, including commands and hotkeys
  </Card>
  <Card icon={<LinkIcon />} href="#" title="Media Embed">
    Include media clips and links with playback options in your notes
  </Card>
  <Card icon={<HelpCircleIcon />} href="#" title="FAQ">
    FAQ for Media Extended
  </Card>
</Cards>

## Special Thanks

A special thanks to [bfcs](https://github.com/bfcs) for their valuable contributions. They have helped fix issues during a long period of inactivity and made attempts to implement YouTube transcript functionality!